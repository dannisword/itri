const LocationStatusEnum = Object.freeze({
  /**
   * 可用 available
   */
  AVAILABLE: 0,
  /**
   * 保留 reserve
   */
  RESERVE: 1,
  /**
   * 鎖定 locking
   */
  LOCKING: 2,
});

export default LocationStatusEnum;
